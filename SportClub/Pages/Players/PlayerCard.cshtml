@page
@model SportClub.Pages.Players.PlayerCardModel

@{
    ViewData["Title"] = "Kartice igraca";
}

<h1>Kartice igraca</h1>

<hr />

<div class="row row-cols-1 row-cols-md-4 g-4">
    @foreach (var player in Model.Player)
    {
        <div class="col">
            <partial name="_PlayerCard" model="player" />
        </div>
    }
</div>